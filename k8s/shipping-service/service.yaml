apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: amazon
  labels:
    app: shipping-service
spec:
  selector:
    app: shipping-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
