apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: amazon
spec:
  type: NodePort
  selector:
    app: shipping-service
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080
