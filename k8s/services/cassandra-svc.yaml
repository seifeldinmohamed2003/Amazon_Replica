apiVersion: v1
kind: Service
metadata:
  name: cassandra
  namespace: amazon
spec:
  clusterIP: None
  selector:
    app: cassandra
  ports:
    - port: 9042
      targetPort: 9042
      protocol: TCP